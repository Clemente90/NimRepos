(.nif24)
(stmts
  (call getZero.0
    (mov (rbx) ret.0)
  )
  (mov (rax) +60)
  (mov (rdi) (rbx))
  (syscall)
  (proc :getZero.0
    (params)
    (result
      (:ret.0 (rax) (i +64))
    )
    (clobber)
    (stmts
      (mov (rax) +0)
      (ret)
    )
  )
)
